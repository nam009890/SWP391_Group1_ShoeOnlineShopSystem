Ở trong file application.properties ( trong phần orther source / defaul package ) có 2 dòng code đã comment như sau :

# Google OAuth2 Configuration

# Mail Configuration (Example for Gmail - USER needs to provide real App Password)

thì hãy copy đoạn này thay 2 dòng trong zalo đã ghim  vào cái file application.properties sau đó clean and build lại dự án rồi mới chạy


